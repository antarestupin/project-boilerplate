version: '2'
services:
    elasticsearch:
        image: elasticsearch:2.4.1
        ports:
            - 9200:9200
            - 9300:9300
        # volumes:
        #     - ./.volumes/{{ path }}/elasticsearch/:/usr/share/elasticsearch/data/
