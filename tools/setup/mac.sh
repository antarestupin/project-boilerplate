#!/usr/bin/env bash

# Installs required dependencies for local development in a Mac OS

# Here is an example of how you can install a dependency if it is missing
#if ! git 2>/dev/null; then
  #echo "Installing git"
  # Install git
#fi
